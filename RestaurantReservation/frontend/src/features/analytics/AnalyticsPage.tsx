import { PagePlaceholder } from '@/components/shared/PagePlaceholder'
import { strings } from '@/lib/strings'

export function AnalyticsPage() {
  return <PagePlaceholder title={strings.nav.analytics} />
}
